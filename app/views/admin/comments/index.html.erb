<div class="title_bar clearfix">
	<h1 class="column"><%= (enclosing_resource ? "Comments for &quot;%s&quot;"%truncate(h(enclosing_resource.title),:length=>10) : 'All comments') %></h1>
	<div class="menu column_right title_options">

	</div>
</div>

<p class="link_list">
	sort by: <%= sort_links %>
</p>

<!-- Use rails convention for collection partials -->

<ul class="index section">
	<%= render :partial => resources %>
</ul>

<%= will_paginate resources, :class => 'pager section' %>

<% content_for :secondary do %>
	<%= render :partial => 'admin/shared/index_sidebar' %>
<% end %>